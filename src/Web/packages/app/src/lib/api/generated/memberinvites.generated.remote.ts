// AUTO-GENERATED - DO NOT EDIT
// Generated by openapi-remote-codegen
// Source: openapi.json

import { getRequestEvent, query, command } from '$app/server';
import { error, redirect } from '@sveltejs/kit';
import { z } from 'zod';

/** Get invite info for the accept page (anonymous). */
export const getInviteInfo = query(z.string(), async (token) => {
  const apiClient = getRequestEvent().locals.apiClient;
  try {
    return await apiClient.memberInvites.getInviteInfo(token);
  } catch (err) {
    const status = (err as any)?.status;
    if (status === 401) { const { url } = getRequestEvent(); throw redirect(302, `/auth/login?returnUrl=${encodeURIComponent(url.pathname + url.search)}`); }
    if (status === 403) throw error(403, 'Forbidden');
    console.error('Error in memberInvites.getInviteInfo:', err);
    throw error(500, 'Failed to get invite info');
  }
});

/** Accept an invite and join the tenant. */
export const acceptInvite = command(z.string(), async (token) => {
  const apiClient = getRequestEvent().locals.apiClient;
  try {
    const result = await apiClient.memberInvites.acceptInvite(token);
    return result;
  } catch (err) {
    const status = (err as any)?.status;
    if (status === 401) { const { url } = getRequestEvent(); throw redirect(302, `/auth/login?returnUrl=${encodeURIComponent(url.pathname + url.search)}`); }
    if (status === 403) throw error(403, 'Forbidden');
    console.error('Error in memberInvites.acceptInvite:', err);
    throw error(500, 'Failed to accept invite');
  }
});

/** List all members of the current tenant. */
export const getMembers = query(async () => {
  const apiClient = getRequestEvent().locals.apiClient;
  try {
    return await apiClient.memberInvites.getMembers();
  } catch (err) {
    const status = (err as any)?.status;
    if (status === 401) { const { url } = getRequestEvent(); throw redirect(302, `/auth/login?returnUrl=${encodeURIComponent(url.pathname + url.search)}`); }
    if (status === 403) throw error(403, 'Forbidden');
    console.error('Error in memberInvites.getMembers:', err);
    throw error(500, 'Failed to get members');
  }
});

/** List followers of the current tenant (members with follower role). */
export const getFollowers = query(async () => {
  const apiClient = getRequestEvent().locals.apiClient;
  try {
    return await apiClient.memberInvites.getFollowers();
  } catch (err) {
    const status = (err as any)?.status;
    if (status === 401) { const { url } = getRequestEvent(); throw redirect(302, `/auth/login?returnUrl=${encodeURIComponent(url.pathname + url.search)}`); }
    if (status === 403) throw error(403, 'Forbidden');
    console.error('Error in memberInvites.getFollowers:', err);
    throw error(500, 'Failed to get followers');
  }
});
