// AUTO-GENERATED - DO NOT EDIT
// Generated by openapi-remote-codegen
// Source: openapi.json

import { getRequestEvent, query, command, form } from '$app/server';
import { error, redirect, invalid } from '@sveltejs/kit';
import { z } from 'zod';
import { UpsertNoteRequestSchema } from '$lib/api/generated/schemas';
import { type UpsertNoteRequest } from '$api';

export const getAll = query(z.object({ from: z.coerce.date().optional(), to: z.coerce.date().optional(), limit: z.number().optional(), offset: z.number().optional(), sort: z.string().optional(), device: z.string().optional(), source: z.string().optional() }).optional(), async (params) => {
  const apiClient = getRequestEvent().locals.apiClient;
  try {
    return await apiClient.notes.getAll(params?.from, params?.to, params?.limit, params?.offset, params?.sort, params?.device, params?.source);
  } catch (err) {
    const status = (err as any)?.status;
    if (status === 401) { const { url } = getRequestEvent(); throw redirect(302, `/auth/login?returnUrl=${encodeURIComponent(url.pathname + url.search)}`); }
    if (status === 403) throw error(403, 'Forbidden');
    console.error('Error in notes.getAll:', err);
    throw error(500, 'Failed to get all');
  }
});

export const create = form(UpsertNoteRequestSchema as any, async (request) => {
  const apiClient = getRequestEvent().locals.apiClient;
  try {
    const result = await apiClient.notes.create(request as UpsertNoteRequest);
    return result;
  } catch (err) {
    const status = (err as any)?.status;
    if (status === 401) { const { url } = getRequestEvent(); throw redirect(302, `/auth/login?returnUrl=${encodeURIComponent(url.pathname + url.search)}`); }
    if (status === 403) throw error(403, 'Forbidden');
    console.error('Error in notes.create:', err);
    throw error(500, 'Failed to create');
  }
});

export const getById = query(z.string(), async (id) => {
  const apiClient = getRequestEvent().locals.apiClient;
  try {
    return await apiClient.notes.getById(id);
  } catch (err) {
    const status = (err as any)?.status;
    if (status === 401) { const { url } = getRequestEvent(); throw redirect(302, `/auth/login?returnUrl=${encodeURIComponent(url.pathname + url.search)}`); }
    if (status === 403) throw error(403, 'Forbidden');
    console.error('Error in notes.getById:', err);
    throw error(500, 'Failed to get by id');
  }
});

export const update = form(z.object({ id: z.string(), request: UpsertNoteRequestSchema }) as any, async ({ id, request }) => {
  const apiClient = getRequestEvent().locals.apiClient;
  try {
    const result = await apiClient.notes.update(id, request as UpsertNoteRequest);
    return result;
  } catch (err) {
    const status = (err as any)?.status;
    if (status === 401) { const { url } = getRequestEvent(); throw redirect(302, `/auth/login?returnUrl=${encodeURIComponent(url.pathname + url.search)}`); }
    if (status === 403) throw error(403, 'Forbidden');
    console.error('Error in notes.update:', err);
    throw error(500, 'Failed to update');
  }
});

export const remove = command(z.string(), async (id) => {
  const apiClient = getRequestEvent().locals.apiClient;
  try {
    await apiClient.notes.delete(id);
    return { success: true };
  } catch (err) {
    const status = (err as any)?.status;
    if (status === 401) { const { url } = getRequestEvent(); throw redirect(302, `/auth/login?returnUrl=${encodeURIComponent(url.pathname + url.search)}`); }
    if (status === 403) throw error(403, 'Forbidden');
    console.error('Error in notes.delete:', err);
    throw error(500, 'Failed to remove');
  }
});
