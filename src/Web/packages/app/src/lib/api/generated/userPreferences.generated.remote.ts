// AUTO-GENERATED - DO NOT EDIT
// Generated by @nocturne/remote-function-codegen
// Source: openapi.json

import { getRequestEvent, query, command } from '$app/server';
import { error, redirect } from '@sveltejs/kit';
import { UpdateUserPreferencesRequestSchema } from '$lib/api/generated/schemas';
import { type UpdateUserPreferencesRequest } from '$api';

/** Get the current user's preferences */
export const getPreferences = query(async () => {
  const { locals } = getRequestEvent();
  const { apiClient } = locals;
  try {
    return await apiClient.userPreferences.getPreferences();
  } catch (err) {
    const status = (err as any)?.status;
    if (status === 401) { const { url } = getRequestEvent(); throw redirect(302, `/auth/login?returnUrl=${encodeURIComponent(url.pathname + url.search)}`); }
    if (status === 403) throw error(403, 'Forbidden');
    console.error('Error in userPreferences.getPreferences:', err);
    throw error(500, 'Failed to get preferences');
  }
});

/** Update the current user's preferences */
export const updatePreferences = command(UpdateUserPreferencesRequestSchema, async (request) => {
  const { locals } = getRequestEvent();
  const { apiClient } = locals;
  try {
    const result = await apiClient.userPreferences.updatePreferences(request as UpdateUserPreferencesRequest);
    return result;
  } catch (err) {
    const status = (err as any)?.status;
    if (status === 401) { const { url } = getRequestEvent(); throw redirect(302, `/auth/login?returnUrl=${encodeURIComponent(url.pathname + url.search)}`); }
    if (status === 403) throw error(403, 'Forbidden');
    console.error('Error in userPreferences.updatePreferences:', err);
    throw error(500, 'Failed to update preferences');
  }
});
