// AUTO-GENERATED - DO NOT EDIT
// Generated by openapi-remote-codegen
// Source: openapi.json

import { getRequestEvent, query, command } from '$app/server';
import { error, redirect } from '@sveltejs/kit';
import { z } from 'zod';
import { CreateChatIdentityLinkRequestSchema } from '$lib/api/generated/schemas';
import { type CreateChatIdentityLinkRequest } from '$api';

/** List active chat identity links for the current tenant. */
export const getLinks = query(async () => {
  const apiClient = getRequestEvent().locals.apiClient;
  try {
    return await apiClient.chatIdentity.getLinks();
  } catch (err) {
    const status = (err as any)?.status;
    if (status === 401) { const { url } = getRequestEvent(); throw redirect(302, `/auth/login?returnUrl=${encodeURIComponent(url.pathname + url.search)}`); }
    if (status === 403) throw error(403, 'Forbidden');
    console.error('Error in chatIdentity.getLinks:', err);
    throw error(500, 'Failed to get links');
  }
});

/** Create a new chat identity link. */
export const createLink = command(CreateChatIdentityLinkRequestSchema, async (request) => {
  const apiClient = getRequestEvent().locals.apiClient;
  try {
    const result = await apiClient.chatIdentity.createLink(request as CreateChatIdentityLinkRequest);
    await Promise.all([
      getLinks(undefined).refresh()
    ]);
    return result;
  } catch (err) {
    const status = (err as any)?.status;
    if (status === 401) { const { url } = getRequestEvent(); throw redirect(302, `/auth/login?returnUrl=${encodeURIComponent(url.pathname + url.search)}`); }
    if (status === 403) throw error(403, 'Forbidden');
    console.error('Error in chatIdentity.createLink:', err);
    throw error(500, 'Failed to create link');
  }
});

/** Revoke (soft-delete) a chat identity link. */
export const revokeLink = command(z.string(), async (id) => {
  const apiClient = getRequestEvent().locals.apiClient;
  try {
    await apiClient.chatIdentity.revokeLink(id);
    await Promise.all([
      getLinks(undefined).refresh()
    ]);
    return { success: true };
  } catch (err) {
    const status = (err as any)?.status;
    if (status === 401) { const { url } = getRequestEvent(); throw redirect(302, `/auth/login?returnUrl=${encodeURIComponent(url.pathname + url.search)}`); }
    if (status === 403) throw error(403, 'Forbidden');
    console.error('Error in chatIdentity.revokeLink:', err);
    throw error(500, 'Failed to revoke link');
  }
});

/** Resolve a platform identity to a Nocturne user. Used by the bot service
to look up which tenant/user a chat message belongs to. */
export const resolve = query(z.object({ platform: z.string().optional(), platformUserId: z.string().optional() }).optional(), async (params) => {
  const apiClient = getRequestEvent().locals.apiClient;
  try {
    return await apiClient.chatIdentity.resolve(params?.platform, params?.platformUserId);
  } catch (err) {
    const status = (err as any)?.status;
    if (status === 401) { const { url } = getRequestEvent(); throw redirect(302, `/auth/login?returnUrl=${encodeURIComponent(url.pathname + url.search)}`); }
    if (status === 403) throw error(403, 'Forbidden');
    console.error('Error in chatIdentity.resolve:', err);
    throw error(500, 'Failed to resolve');
  }
});
