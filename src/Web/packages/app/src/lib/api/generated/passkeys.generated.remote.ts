// AUTO-GENERATED - DO NOT EDIT
// Generated by @nocturne/remote-function-codegen
// Source: openapi.json

import { getRequestEvent, query, command } from '$app/server';
import { error, redirect } from '@sveltejs/kit';
import { z } from 'zod';
import { PasskeyRegisterOptionsRequestSchema, PasskeyRegisterCompleteRequestSchema, PasskeyLoginOptionsRequestSchema, PasskeyLoginCompleteRequestSchema, RecoveryVerifyRequestSchema } from '$lib/api/generated/schemas';
import { type PasskeyRegisterOptionsRequest, type PasskeyRegisterCompleteRequest, type PasskeyLoginOptionsRequest, type PasskeyLoginCompleteRequest, type RecoveryVerifyRequest } from '$api';

/** Generate registration options for a new passkey credential */
export const registerOptions = command(PasskeyRegisterOptionsRequestSchema, async (request) => {
  const { locals } = getRequestEvent();
  const { apiClient } = locals;
  try {
    const result = await apiClient.passkey.registerOptions(request as PasskeyRegisterOptionsRequest);
    return result;
  } catch (err) {
    const status = (err as any)?.status;
    if (status === 401) { const { url } = getRequestEvent(); throw redirect(302, `/auth/login?returnUrl=${encodeURIComponent(url.pathname + url.search)}`); }
    if (status === 403) throw error(403, 'Forbidden');
    console.error('Error in passkey.registerOptions:', err);
    throw error(500, 'Failed to register options');
  }
});

/** Complete passkey registration with attestation response */
export const registerComplete = command(PasskeyRegisterCompleteRequestSchema, async (request) => {
  const { locals } = getRequestEvent();
  const { apiClient } = locals;
  try {
    const result = await apiClient.passkey.registerComplete(request as PasskeyRegisterCompleteRequest);
    await Promise.all([
      listCredentials(undefined).refresh()
    ]);
    return result;
  } catch (err) {
    const status = (err as any)?.status;
    if (status === 401) { const { url } = getRequestEvent(); throw redirect(302, `/auth/login?returnUrl=${encodeURIComponent(url.pathname + url.search)}`); }
    if (status === 403) throw error(403, 'Forbidden');
    console.error('Error in passkey.registerComplete:', err);
    throw error(500, 'Failed to register complete');
  }
});

/** Generate discoverable assertion options (no username required) */
export const discoverableLoginOptions = command(async () => {
  const { locals } = getRequestEvent();
  const { apiClient } = locals;
  try {
    const result = await apiClient.passkey.discoverableLoginOptions();
    return result;
  } catch (err) {
    const status = (err as any)?.status;
    if (status === 401) { const { url } = getRequestEvent(); throw redirect(302, `/auth/login?returnUrl=${encodeURIComponent(url.pathname + url.search)}`); }
    if (status === 403) throw error(403, 'Forbidden');
    console.error('Error in passkey.discoverableLoginOptions:', err);
    throw error(500, 'Failed to discoverable login options');
  }
});

/** Generate assertion options for a specific user */
export const loginOptions = command(PasskeyLoginOptionsRequestSchema, async (request) => {
  const { locals } = getRequestEvent();
  const { apiClient } = locals;
  try {
    const result = await apiClient.passkey.loginOptions(request as PasskeyLoginOptionsRequest);
    return result;
  } catch (err) {
    const status = (err as any)?.status;
    if (status === 401) { const { url } = getRequestEvent(); throw redirect(302, `/auth/login?returnUrl=${encodeURIComponent(url.pathname + url.search)}`); }
    if (status === 403) throw error(403, 'Forbidden');
    console.error('Error in passkey.loginOptions:', err);
    throw error(500, 'Failed to login options');
  }
});

/** Complete passkey login with assertion response */
export const loginComplete = command(PasskeyLoginCompleteRequestSchema, async (request) => {
  const { locals } = getRequestEvent();
  const { apiClient } = locals;
  try {
    const result = await apiClient.passkey.loginComplete(request as PasskeyLoginCompleteRequest);
    return result;
  } catch (err) {
    const status = (err as any)?.status;
    if (status === 401) { const { url } = getRequestEvent(); throw redirect(302, `/auth/login?returnUrl=${encodeURIComponent(url.pathname + url.search)}`); }
    if (status === 403) throw error(403, 'Forbidden');
    console.error('Error in passkey.loginComplete:', err);
    throw error(500, 'Failed to login complete');
  }
});

/** Verify a recovery code and issue a restricted recovery session */
export const recoveryVerify = command(RecoveryVerifyRequestSchema, async (request) => {
  const { locals } = getRequestEvent();
  const { apiClient } = locals;
  try {
    const result = await apiClient.passkey.recoveryVerify(request as RecoveryVerifyRequest);
    return result;
  } catch (err) {
    const status = (err as any)?.status;
    if (status === 401) { const { url } = getRequestEvent(); throw redirect(302, `/auth/login?returnUrl=${encodeURIComponent(url.pathname + url.search)}`); }
    if (status === 403) throw error(403, 'Forbidden');
    console.error('Error in passkey.recoveryVerify:', err);
    throw error(500, 'Failed to recovery verify');
  }
});

/** List all passkey credentials for the authenticated user */
export const listCredentials = query(async () => {
  const { locals } = getRequestEvent();
  const { apiClient } = locals;
  try {
    return await apiClient.passkey.listCredentials();
  } catch (err) {
    const status = (err as any)?.status;
    if (status === 401) { const { url } = getRequestEvent(); throw redirect(302, `/auth/login?returnUrl=${encodeURIComponent(url.pathname + url.search)}`); }
    if (status === 403) throw error(403, 'Forbidden');
    console.error('Error in passkey.listCredentials:', err);
    throw error(500, 'Failed to list credentials');
  }
});

/** Remove a passkey credential. Cannot remove the last credential if user has no OIDC link. */
export const removeCredential = command(z.string(), async (id) => {
  const { locals } = getRequestEvent();
  const { apiClient } = locals;
  try {
    await apiClient.passkey.removeCredential(id);
    await Promise.all([
      listCredentials(undefined).refresh()
    ]);
    return { success: true };
  } catch (err) {
    const status = (err as any)?.status;
    if (status === 401) { const { url } = getRequestEvent(); throw redirect(302, `/auth/login?returnUrl=${encodeURIComponent(url.pathname + url.search)}`); }
    if (status === 403) throw error(403, 'Forbidden');
    console.error('Error in passkey.removeCredential:', err);
    throw error(500, 'Failed to remove credential');
  }
});

/** Regenerate recovery codes for the authenticated user. Invalidates all existing codes. */
export const regenerateRecoveryCodes = command(async () => {
  const { locals } = getRequestEvent();
  const { apiClient } = locals;
  try {
    const result = await apiClient.passkey.regenerateRecoveryCodes();
    await Promise.all([
      getRecoveryStatus(undefined).refresh()
    ]);
    return result;
  } catch (err) {
    const status = (err as any)?.status;
    if (status === 401) { const { url } = getRequestEvent(); throw redirect(302, `/auth/login?returnUrl=${encodeURIComponent(url.pathname + url.search)}`); }
    if (status === 403) throw error(403, 'Forbidden');
    console.error('Error in passkey.regenerateRecoveryCodes:', err);
    throw error(500, 'Failed to regenerate recovery codes');
  }
});

/** Get the count of remaining recovery codes for the authenticated user */
export const getRecoveryStatus = query(async () => {
  const { locals } = getRequestEvent();
  const { apiClient } = locals;
  try {
    return await apiClient.passkey.getRecoveryStatus();
  } catch (err) {
    const status = (err as any)?.status;
    if (status === 401) { const { url } = getRequestEvent(); throw redirect(302, `/auth/login?returnUrl=${encodeURIComponent(url.pathname + url.search)}`); }
    if (status === 403) throw error(403, 'Forbidden');
    console.error('Error in passkey.getRecoveryStatus:', err);
    throw error(500, 'Failed to get recovery status');
  }
});

/** Returns whether the instance is currently in recovery mode.
Recovery mode activates when active subjects exist that have no
passkey credential and no OIDC binding (orphaned after upgrade). */
export const getRecoveryModeStatus = query(async () => {
  const { locals } = getRequestEvent();
  const { apiClient } = locals;
  try {
    return await apiClient.passkey.getRecoveryModeStatus();
  } catch (err) {
    const status = (err as any)?.status;
    if (status === 401) { const { url } = getRequestEvent(); throw redirect(302, `/auth/login?returnUrl=${encodeURIComponent(url.pathname + url.search)}`); }
    if (status === 403) throw error(403, 'Forbidden');
    console.error('Error in passkey.getRecoveryModeStatus:', err);
    throw error(500, 'Failed to get recovery mode status');
  }
});
