// AUTO-GENERATED - DO NOT EDIT
// Generated by @nocturne/remote-function-codegen
// Source: openapi.json

import { getRequestEvent, query, command } from '$app/server';
import { error, redirect } from '@sveltejs/kit';
import { z } from 'zod';
import { CreateTenantRequestSchema, UpdateTenantRequestSchema, AddMemberRequestSchema } from '$lib/api/generated/schemas';
import { type CreateTenantRequest, type UpdateTenantRequest, type AddMemberRequest } from '$api';

export const getAll = query(async () => {
  const { locals } = getRequestEvent();
  const { apiClient } = locals;
  try {
    return await apiClient.tenant.getAll();
  } catch (err) {
    const status = (err as any)?.status;
    if (status === 401) { const { url } = getRequestEvent(); throw redirect(302, `/auth/login?returnUrl=${encodeURIComponent(url.pathname + url.search)}`); }
    if (status === 403) throw error(403, 'Forbidden');
    console.error('Error in tenant.getAll:', err);
    throw error(500, 'Failed to get all');
  }
});

export const create = command(CreateTenantRequestSchema, async (request) => {
  const { locals } = getRequestEvent();
  const { apiClient } = locals;
  try {
    const result = await apiClient.tenant.create(request as CreateTenantRequest);
    await Promise.all([
      getAll(undefined).refresh()
    ]);
    return result;
  } catch (err) {
    const status = (err as any)?.status;
    if (status === 401) { const { url } = getRequestEvent(); throw redirect(302, `/auth/login?returnUrl=${encodeURIComponent(url.pathname + url.search)}`); }
    if (status === 403) throw error(403, 'Forbidden');
    console.error('Error in tenant.create:', err);
    throw error(500, 'Failed to create');
  }
});

export const getById = query(z.string(), async (id) => {
  const { locals } = getRequestEvent();
  const { apiClient } = locals;
  try {
    return await apiClient.tenant.getById(id);
  } catch (err) {
    const status = (err as any)?.status;
    if (status === 401) { const { url } = getRequestEvent(); throw redirect(302, `/auth/login?returnUrl=${encodeURIComponent(url.pathname + url.search)}`); }
    if (status === 403) throw error(403, 'Forbidden');
    console.error('Error in tenant.getById:', err);
    throw error(500, 'Failed to get by id');
  }
});

export const update = command(z.object({ id: z.string(), request: UpdateTenantRequestSchema }), async ({ id, request }) => {
  const { locals } = getRequestEvent();
  const { apiClient } = locals;
  try {
    const result = await apiClient.tenant.update(id, request as UpdateTenantRequest);
    await Promise.all([
      getAll(undefined).refresh(),
      getById(id).refresh()
    ]);
    return result;
  } catch (err) {
    const status = (err as any)?.status;
    if (status === 401) { const { url } = getRequestEvent(); throw redirect(302, `/auth/login?returnUrl=${encodeURIComponent(url.pathname + url.search)}`); }
    if (status === 403) throw error(403, 'Forbidden');
    console.error('Error in tenant.update:', err);
    throw error(500, 'Failed to update');
  }
});

export const addMember = command(z.object({ id: z.string(), request: AddMemberRequestSchema }), async ({ id, request }) => {
  const { locals } = getRequestEvent();
  const { apiClient } = locals;
  try {
    await apiClient.tenant.addMember(id, request as AddMemberRequest);
    await Promise.all([
      getById(id).refresh()
    ]);
    return { success: true };
  } catch (err) {
    const status = (err as any)?.status;
    if (status === 401) { const { url } = getRequestEvent(); throw redirect(302, `/auth/login?returnUrl=${encodeURIComponent(url.pathname + url.search)}`); }
    if (status === 403) throw error(403, 'Forbidden');
    console.error('Error in tenant.addMember:', err);
    throw error(500, 'Failed to add member');
  }
});

export const removeMember = command(z.object({ id: z.string(), subjectId: z.string() }), async ({ id, subjectId }) => {
  const { locals } = getRequestEvent();
  const { apiClient } = locals;
  try {
    await apiClient.tenant.removeMember(id, subjectId);
    await Promise.all([
      getById(id).refresh()
    ]);
    return { success: true };
  } catch (err) {
    const status = (err as any)?.status;
    if (status === 401) { const { url } = getRequestEvent(); throw redirect(302, `/auth/login?returnUrl=${encodeURIComponent(url.pathname + url.search)}`); }
    if (status === 403) throw error(403, 'Forbidden');
    console.error('Error in tenant.removeMember:', err);
    throw error(500, 'Failed to remove member');
  }
});
