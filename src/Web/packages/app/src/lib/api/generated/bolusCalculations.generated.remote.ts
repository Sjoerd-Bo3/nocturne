// AUTO-GENERATED - DO NOT EDIT
// Generated by openapi-remote-codegen
// Source: openapi.json

import { getRequestEvent, query, command, form } from '$app/server';
import { error, redirect, invalid } from '@sveltejs/kit';
import { z } from 'zod';
import { UpsertBolusCalculationRequestSchema } from '$lib/api/generated/schemas';
import { type UpsertBolusCalculationRequest } from '$api';

export const getAll = query(z.object({ from: z.coerce.date().optional(), to: z.coerce.date().optional(), limit: z.number().optional(), offset: z.number().optional(), sort: z.string().optional(), device: z.string().optional(), source: z.string().optional() }).optional(), async (params) => {
  const apiClient = getRequestEvent().locals.apiClient;
  try {
    return await apiClient.bolusCalculations.getAll(params?.from, params?.to, params?.limit, params?.offset, params?.sort, params?.device, params?.source);
  } catch (err) {
    const status = (err as any)?.status;
    if (status === 401) { const { url } = getRequestEvent(); throw redirect(302, `/auth/login?returnUrl=${encodeURIComponent(url.pathname + url.search)}`); }
    if (status === 403) throw error(403, 'Forbidden');
    console.error('Error in bolusCalculations.getAll:', err);
    throw error(500, 'Failed to get all');
  }
});

export const create = form(UpsertBolusCalculationRequestSchema as any, async (request) => {
  const apiClient = getRequestEvent().locals.apiClient;
  try {
    const result = await apiClient.bolusCalculations.create(request as UpsertBolusCalculationRequest);
    return result;
  } catch (err) {
    const status = (err as any)?.status;
    if (status === 401) { const { url } = getRequestEvent(); throw redirect(302, `/auth/login?returnUrl=${encodeURIComponent(url.pathname + url.search)}`); }
    if (status === 403) throw error(403, 'Forbidden');
    console.error('Error in bolusCalculations.create:', err);
    throw error(500, 'Failed to create');
  }
});

export const getById = query(z.string(), async (id) => {
  const apiClient = getRequestEvent().locals.apiClient;
  try {
    return await apiClient.bolusCalculations.getById(id);
  } catch (err) {
    const status = (err as any)?.status;
    if (status === 401) { const { url } = getRequestEvent(); throw redirect(302, `/auth/login?returnUrl=${encodeURIComponent(url.pathname + url.search)}`); }
    if (status === 403) throw error(403, 'Forbidden');
    console.error('Error in bolusCalculations.getById:', err);
    throw error(500, 'Failed to get by id');
  }
});

export const update = form(z.object({ id: z.string(), request: UpsertBolusCalculationRequestSchema }) as any, async ({ id, request }) => {
  const apiClient = getRequestEvent().locals.apiClient;
  try {
    const result = await apiClient.bolusCalculations.update(id, request as UpsertBolusCalculationRequest);
    return result;
  } catch (err) {
    const status = (err as any)?.status;
    if (status === 401) { const { url } = getRequestEvent(); throw redirect(302, `/auth/login?returnUrl=${encodeURIComponent(url.pathname + url.search)}`); }
    if (status === 403) throw error(403, 'Forbidden');
    console.error('Error in bolusCalculations.update:', err);
    throw error(500, 'Failed to update');
  }
});

export const remove = command(z.string(), async (id) => {
  const apiClient = getRequestEvent().locals.apiClient;
  try {
    await apiClient.bolusCalculations.delete(id);
    return { success: true };
  } catch (err) {
    const status = (err as any)?.status;
    if (status === 401) { const { url } = getRequestEvent(); throw redirect(302, `/auth/login?returnUrl=${encodeURIComponent(url.pathname + url.search)}`); }
    if (status === 403) throw error(403, 'Forbidden');
    console.error('Error in bolusCalculations.delete:', err);
    throw error(500, 'Failed to remove');
  }
});
